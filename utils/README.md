Get the certificate by running getcert.sh. Change the domains in the script as needed.

Create the pfx with:

```bash
./makepfx.sh ~/.acme.sh/msbaeke.com_ecc/msbaeke.com.cer ~/.acme.sh/msbaeke.com_ecc/msbaeke.com.key
```
